manifest {
    homePage = 'https://github.com/icgc-argo/variant-calling-tools'
    description = 'A collection of variant calling tools / modules used in ICGC ARGO variant calling workflows'
    nextflowVersion = '>=20.10'
}

docker {
    enabled = true
    runOptions = '-u \$(id -u):\$(id -g)'
}
